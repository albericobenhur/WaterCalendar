engine-strict=true
# Mantemos optional deps habilitadas (oxc-parser precisa do binding nativo)
# Se houver novos problemas, poderíamos filtrar via npm pkg overrides.
fund=false
audit=false
# (Opcional) Para debug, você pode ativar logs verbosos:
# loglevel=warn
